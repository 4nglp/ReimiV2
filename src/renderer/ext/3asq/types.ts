export type Pinned = {
  title: string;
  posterUrl: string;
  path: string;
};

export type Latest = {
  title: string;
  posterUrl: string;
  path: string;
  latestChapter: string;
};

export type Results = {
  title: string;
  posterUrl: string;
  path: string;
};
